fn fib_rec(x) -> i64 {
	if (x == 0) || (x == 1) {
		x
	} else {
		fib_rec(x - 1) + fib_rec(x - 2)
	}
}

/* fn fib_norec(x) -> i64 {
	if (x == 0) || (x == 1) {
		x
	}
	
	let n_2 = 1;
	let n_1 = 3;
	let n = 0;
	for (let i = 2; i <= x; i += 1) {
		n = 3 * n_1 - n_2;
		n_2 = n_1;
		n_1 = n;
	}

	n
} */

/* fn value() -> i64 {
	let x = 5 + 5 * 5;
	
	2 + if 5 > 7 {
		2*x*x*x - 5*x*x + 3*x - 17
	} else {
		-2
	}
} */

fn main() -> i64 {
	// value()
	fib_rec(16)
}